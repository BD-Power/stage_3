events {}

http {
    upstream search_cluster {
        server search1:8080;
        server search2:8080;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://search_cluster;
        }
    }
}
